# Expert Network Search Copilot — Python dependencies
# Python >= 3.9 required

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Data validation
pydantic>=2.7.0
pydantic-settings>=2.3.0

# PostgreSQL (read-only access to candidate profiles DB)
psycopg2-binary>=2.9.9

# Vector database — local, persistent, no external service needed
chromadb>=0.5.0

# Embeddings — local sentence-transformer model (~90 MB, downloaded on first run)
sentence-transformers>=3.0.0

# LLM access via OpenRouter (OpenAI-compatible API)
openai>=1.30.0

# HTTP client
httpx>=0.27.0

# Utilities
python-dotenv>=1.0.0
tenacity>=8.3.0      # Retry logic for LLM/API calls
tqdm>=4.66.0         # Progress bars during ingestion
numpy>=1.24.0

# Async support (FastAPI dependency)
anyio>=4.0.0
